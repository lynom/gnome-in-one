import { useState } from "react";

const BLOCKS = [
    { id: "ramp", label: "ramp", icon: "R", color: "#ffffff" }
];

export default function GameUI() {

    return (
        <div>
            {/* playfield + play button*/}
            
        </div>
    );
}

